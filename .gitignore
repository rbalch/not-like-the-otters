# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
develop-eggs/
dist/
*.egg-info/
*.egg
.eggs/
.hypothesis/
.coverage
.coverage.*
htmlcov/
.tox/
coverage.xml
nosetests.xml

# JS / Node
node_modules/
dist/
build/
.npm-install-changed.json

# Env / secrets
*.env*
key.json
secret.pem

# macOS
**/.DS_Store

# IDEs
.idea/
.vscode/settings.json

# devcontainer
.devcontainer/devcontainer-lock.json

# Linting caches
.ruff_cache/
.pytest_cache/

# Logs
*.log

# codegraph — a tree-sitter index is build output, and it rebuilds itself on watch
.codegraph/

# Claude Code — project settings are shared; local overrides are not
.claude/settings.local.json

# Build-loop working papers. The reviewer rewrites these fresh each round, and the
# durable record is docs/ledger-findings.md — triaged findings, not raw reviews.
# Kept on disk so the loop can read the previous round; never committed.
review.md
review.json

# Misc
*.sqlite3
*.mo
*.pot
docs/_build/
target/
